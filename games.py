def calc_games(win_pcts):
    """
    Input:
        win_pcts (list) - list of win percentages based on score for each week

    Returns:
        games (integer) - number of games played
    """
    return len(win_pcts)